

# Rename "Command" to "Daily Command" in Sidebar

Single change: update the `coreItems` array in `src/components/ProductSidebar.tsx`, changing the title from `"Command"` to `"Daily Command"`.

No other files need changes -- the route and page component remain the same.

